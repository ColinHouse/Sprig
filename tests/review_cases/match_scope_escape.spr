variant Expr:
    Literal(value: Int)
    Add(left: Expr, right: Expr)

func evaluate(expr: Expr) -> Int:
    match expr:
        case Expr.Literal as node:
            pass
        case Expr.Add as node:
            pass
    return node.value
