variant Signal:
    Stop
    Go
    Pause

func label(signal: Signal) -> String:
    match signal:
        case Signal.Stop:
            return "stop"
        case Signal.Go:
            return "go"
        case Signal.Pause:
            return "pause"

print(label(Signal.Pause))
